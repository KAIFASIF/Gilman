import React from "react";

const Search = () => {
  return (
    <div className="bg-white w-full flex p-4">
      <p>Search</p>
      <p>Search</p>
    </div>
  );
};

export default React.memo(Search);
